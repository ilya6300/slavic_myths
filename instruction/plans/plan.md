### Баги

1. ~~Кликнув по спящему коту, он не просыпается, но моналог идёт и монетки прибавляются~~ **done** — `useOnboardingBootstrap` ставил `catSleeping` без `catSleepReason`; `registerActivity` не будил. Фикс: `enterAfkSleep()` + будить любой не-tired сон.

Для разработчика

### Улучшение анимации

2. ~~В мге чтобы вдохнуть жизнь в кота и домового прикручена анимация (как я понял дыхания), но кот прям вообще увеличивается не естественно~~ **done** — `cat-breath`: `scale(1.02)` → `translateY(-1.5px)` (как у домового `brownie-bob`, без роста спрайта).

для дизайнера

